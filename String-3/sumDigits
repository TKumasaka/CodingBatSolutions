public int sumDigits(String str) {
  int sum = 0;
  for(int i = 0; i < str.length(); i++){
    Character next = str.charAt(i);
    if(Character.isDigit(next)){
      sum += Integer.parseInt(Character.toString(next));
    }
  }
  return sum;
}
