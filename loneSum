public int loneSum(int a, int b, int c) {
  int sum = a+b+c;
  if(a == b && b == c && a == c){
    sum = 0;}
  else if(a==b){
    sum = c;}
  else if(b==c){
    sum = a;}
  else if(c==a){
    sum = b;}
  return sum;
}
